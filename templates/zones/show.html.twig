{% extends 'base.html.twig' %}

{% block title %}Zones{% endblock %}

{% block body %}
    <h1>Zones</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ZoneId</th>
                <td>{{ zone.zoneId }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ zone.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ zone.description }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ zone.capacity }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ zone.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zones_index') }}">back to list</a>

    <a href="{{ path('app_zones_edit', {'zoneId': zone.zoneId}) }}">edit</a>

    {{ include('zones/_delete_form.html.twig') }}
{% endblock %}
