<form method="post" action="{{ path('app_resto_back_delete', {'idResto': resto.idResto}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resto.idResto) }}">
    <button class="btn">Delete</button>
</form>
