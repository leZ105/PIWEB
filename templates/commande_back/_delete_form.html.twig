<form method="post" action="{{ path('app_commande_back_delete', {'numc': commande.numc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mt-3">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.numc) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form>
