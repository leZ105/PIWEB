<form method="post" action="{{ path('app_categorie_menu_back_delete', {'idCategorie': categorie_menu.idCategorie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_menu.idCategorie) }}">
    <button class="btn">Delete</button>
</form>
