{% extends 'baseBack.html.twig' %}

{% block title %}Edit Zones{% endblock %}

{% block body %}
    <h1>Edit Zones</h1>

    {{ include('zones_back/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_zones_back_index') }}">back to list</a>

    {{ include('zones_back/_delete_form.html.twig') }}
{% endblock %}
