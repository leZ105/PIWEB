<form method="post" action="{{ path('app_tables_back_delete', {'tableId': table.tableId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ table.tableId) }}">
    <button class="btn">Delete</button>
</form>
