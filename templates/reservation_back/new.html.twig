{% extends 'baseBack.html.twig' %}

{% block title %}New Reservation{% endblock %}

{% block body %}
    <h1>Create new Reservation</h1>

    {{ include('reservation_back/_form.html.twig') }}

    <a href="{{ path('app_reservation_back_index') }}">back to list</a>
{% endblock %}
