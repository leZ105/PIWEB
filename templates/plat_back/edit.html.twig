{% extends 'baseBack.html.twig' %}

{% block title %}Edit Plat{% endblock %}

{% block body %}
    <h1>Edit Plat</h1>

    {{ include('plat_back/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_plat_back_index') }}">back to list</a>

    {{ include('plat_back/_delete_form.html.twig') }}
{% endblock %}
